{
  "scripts": {
    "prestart": "cd sources && npm run build",
    "start": "./ragemp-server.exe | pino-pretty",
    "start:server": "./ragemp-server.exe | pino-pretty"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
